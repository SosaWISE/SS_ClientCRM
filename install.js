// "use strict";

// var fs = require('fs'),
//   precommitPath = '.git/hooks/pre-commit';

// // copy pre-commit to git hooks and make executable
// fs.createReadStream('pre-commit.sh').pipe(fs.createWriteStream(precommitPath));
// fs.chmodSync('.git/hooks/pre-commit', '755');
