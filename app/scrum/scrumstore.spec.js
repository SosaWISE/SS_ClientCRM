/* global describe, it, expect, beforeEach */
define('src/scrum/scrumstore.spec', [
  'src/scrum/scrumstore',
], function(
  ScrumStore
) {
  "use strict";

  describe('scrumstore', function() {
    var store;
    beforeEach(function() {
      store = new ScrumStore();
    });

    it('should ', function() {
      expect(null).toBe(null);
    });

    describe('storyUpdated', function() {
      beforeEach(function() {

      });

      it('should ', function() {
        expect(null).toBe(null);
      });
    });
  });
});
