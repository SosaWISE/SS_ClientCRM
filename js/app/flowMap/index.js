define(['./flowUtil','./map','./rectangle','./marker'],
function (flowUtil, Map, Rectangle, Marker) {
	"use strict";

	return {
		util: flowUtil,
		Map: Map,
		Rectangle: Rectangle,
		Marker: Marker,
	};
});
